name: Update Release Plan

on:
  schedule:
    - cron: '0 8 * * 1'  # Every Monday at 8 AM UTC
  workflow_dispatch:  # Allow manual triggers
  push:
    branches:
      - main
    paths:
      - 'release_manager.py'
      - 'auto_scheduler.py'
      - 'fit_predictor_adapter.py'
      - 'lib/release_fit_predictor/**'

jobs:
  update-plan:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH for private submodule
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SUBMODULE_DEPLOY_KEY }}

      - name: Checkout submodules
        run: git submodule update --init --recursive

      - name: Update submodules
        run: git submodule update --remote --merge

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Generate release plan
        env:
          JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}
        run: |
          python3 release_manager.py

      - name: Commit and push if changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update release plan [automated]"
          file_pattern: "release-manager.html"
          commit_user_name: "GitHub Actions"
          commit_user_email: "actions@github.com"
